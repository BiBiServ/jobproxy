machine:
  java:
    version: oraclejdk8
  services:
      - docker
dependencies:        
  override:
      - cd jobproxy && mvn --fail-never dependency:go-offline || true
test:
  override:
      - cd jobproxy && mvn clean compile install package && mvn test
